# ================================================================
# FRONTEND-SPECIFIC ENVIRONMENT TEMPLATE
# ================================================================
# 
# This file contains environment variables specific to the Next.js frontend
# These variables supplement the root .env configuration
#
# Note: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Be careful not to include sensitive information in NEXT_PUBLIC_ variables
#
# ================================================================

# ==================
# NEXT.JS CONFIGURATION
# ==================
# Next.js specific settings
NEXT_TELEMETRY_DISABLED="1"                # Disable Next.js telemetry
FAST_REFRESH="true"                         # Enable React Fast Refresh
NEXT_WEBPACK_USEFILELOADER="false"         # Use Next.js built-in file loader

# Build configuration
NEXT_CACHE_SIZE="50"                        # Next.js cache size in MB
NEXT_CACHE_TTL="86400"                      # Cache TTL in seconds
SKIP_TYPE_CHECK="false"                     # Skip TypeScript checking during build
SKIP_LINT="false"                           # Skip linting during build

# Next.js phases and build
NEXT_PHASE=""                               # Next.js build phase (auto-set)
__NEXT_BUILD_ID=""                          # Build ID (auto-generated)
__NEXT_PRIVATE_STANDALONE_CONFIG=""         # Standalone config (auto-generated)

# Server Actions encryption
NEXT_SERVER_ACTIONS_ENCRYPTION_KEY=""       # Encryption key for server actions

# ==================
# CLIENT-SIDE API CONFIGURATION
# ==================
# These NEXT_PUBLIC_ variables are accessible in the browser
NEXT_PUBLIC_API_URL="/api"                  # API endpoint for client-side requests
NEXT_PUBLIC_BASE_URL="http://localhost:3000"  # Application base URL
NEXT_PUBLIC_API_TIMEOUT="30000"             # Client-side API timeout
NEXT_PUBLIC_API_RETRY_ATTEMPTS="3"          # Client-side retry attempts

# ==================
# CLIENT-SIDE APPLICATION CONFIG
# ==================
NEXT_PUBLIC_ENVIRONMENT="development"       # Environment name for client
NEXT_PUBLIC_VERSION="2.0.0"                # Version displayed in UI
NEXT_PUBLIC_APP_VERSION="2.0.0"            # Legacy compatibility
NEXT_PUBLIC_DEBUG="false"                  # Enable client-side debugging
NEXT_PUBLIC_DEV_MODE="true"                # Development mode features

# ==================
# CLIENT-SIDE FIREBASE CONFIG
# ==================
# Firebase SDK configuration (safe to expose)
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-firebase-project"
NEXT_PUBLIC_FIREBASE_API_KEY="your-firebase-api-key"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-firebase-app-id"

# Alternative: Complete Firebase config as JSON string
NEXT_PUBLIC_FIREBASE_CONFIG=""              # Complete Firebase config JSON

# ==================
# CLIENT-SIDE SHOPIFY CONFIG
# ==================
# Shopify Storefront API (safe to expose)
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN="your-store.myshopify.com"
NEXT_PUBLIC_SHOPIFY_STOREFRONT_ACCESS_TOKEN="your-storefront-token"

# ==================
# CLIENT-SIDE ALGOLIA SEARCH CONFIG
# ==================
# Algolia InstantSearch configuration (safe to expose - search-only keys)
NEXT_PUBLIC_ALGOLIA_APP_ID="your-algolia-app-id"
NEXT_PUBLIC_ALGOLIA_SEARCH_API_KEY="your-search-only-api-key"
NEXT_PUBLIC_ALGOLIA_INDEX_NAME="products_production"

# ==================
# CLIENT-SIDE MONITORING
# ==================
# Error monitoring for client-side errors
NEXT_PUBLIC_SENTRY_DSN=""                   # Sentry DSN for browser errors
NEXT_PUBLIC_SENTRY_RELEASE=""               # Release identifier

# ==================
# FRONTEND DEVELOPMENT
# ==================
# Development server configuration
PORT="3000"                                 # Development server port
HOSTNAME="localhost"                        # Development server hostname

# Development features
NEXT_PUBLIC_DEV_MODE="true"                # Enable development features

# ================================================================
# FRONTEND SECURITY NOTES
# ================================================================
#
# NEVER include sensitive information in NEXT_PUBLIC_ variables:
# ❌ Database passwords
# ❌ API secrets or private keys
# ❌ Admin access tokens
# ❌ Private Firebase configuration
# ❌ Server-side only secrets
#
# Safe for NEXT_PUBLIC_ (these are meant to be public):
# ✅ Firebase client configuration
# ✅ Shopify Storefront API tokens
# ✅ Public API endpoints
# ✅ Application version and environment
# ✅ Feature flags for UI
#
# ================================================================

# ================================================================
# FRONTEND BUILD OPTIMIZATION
# ================================================================
#
# For production builds:
# - Set NEXT_TELEMETRY_DISABLED="1"
# - Enable type checking: SKIP_TYPE_CHECK="false"
# - Enable linting: SKIP_LINT="false"
# - Disable debug mode: NEXT_PUBLIC_DEBUG="false"
# - Set appropriate cache sizes for your deployment
#
# ================================================================